diff a/server.js b/server.js	(rejected hunks)
@@ -89,4 +89,4 @@
 })
 
 //server.listen(8080);
-app.listen(PORT,()=>{debug(debugmodule, 'Listening on port'+PORT)});
+app.listen(PORT,()=>{debug(debugmodule, 'Listening on port '+PORT)});
